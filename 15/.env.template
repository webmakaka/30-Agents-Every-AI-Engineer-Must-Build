# ===========================================================================
# .env.template — Chapter 15: Education and Knowledge Agents
# Book: 30 Agents Every AI Engineer Must Build (Packt Publishing)
# Author: Imran Ahmad
#
# INSTRUCTIONS:
#   1. Copy this file to .env:    cp .env.template .env
#   2. Replace the placeholder with your actual OpenAI API key.
#   3. The notebook detects the key automatically on startup.
#
# NO API KEY? No problem. Leave .env absent or empty and the notebook
# runs in Simulation Mode with pre-authored mock responses (Ch.15, p. 2).
#
# Three-tier key resolution (§3 — Strategy):
#   Tier 1: .env file (this file, loaded via python-dotenv)
#   Tier 2: Shell environment variable (export OPENAI_API_KEY=<your-key>)
#   Tier 3: Interactive getpass prompt (Jupyter only)
#   Fallback: Simulation Mode — no API key required
# ===========================================================================

OPENAI_API_KEY=your-openai-api-key-here

# Multi-provider support (added for Anthropic and Google Gemini testing)
# Set LLM_PROVIDER to choose: openai | anthropic | google | auto
# "auto" (default) uses the first available key in order: OpenAI → Anthropic → Google
LLM_PROVIDER=auto

ANTHROPIC_API_KEY=
GOOGLE_API_KEY=
