# Chapter 04 — OPENAI provider dependencies
# Install: pip install -r requirements.txt -r requirements-openai.txt

# Base dependencies (shared across all providers)
-r requirements.txt

# OpenAI provider
openai>=1.40.0,<2.0.0
langchain-openai>=0.3.0
