# Chapter 16: Embodied and Physical World Agents
# Zero-Hardcode Policy: Never commit this file with a real key.
#
# To use Live Mode:
#   1. Copy this file to .env
#   2. Replace the placeholder with your OpenAI API key
#   3. Ensure .env is in .gitignore (it is by default)
#
# To use Simulation Mode:
#   Simply run the notebook without creating .env.
#   All agents will use MockChatOpenAI with chapter-accurate mock data.

OPENAI_API_KEY=your-openai-api-key-here

# Multi-provider support (added for Anthropic and Google Gemini testing)
# Set LLM_PROVIDER to choose: openai | anthropic | google | auto
# "auto" (default) uses the first available key in order: OpenAI → Anthropic → Google
LLM_PROVIDER=auto

ANTHROPIC_API_KEY=
GOOGLE_API_KEY=
