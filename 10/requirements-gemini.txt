# Chapter 10 — GEMINI provider dependencies
# Install: pip install -r requirements.txt -r requirements-gemini.txt

# Base dependencies (shared across all providers)
-r requirements.txt

# Google Gemini provider
google-generativeai>=0.8.0
langchain-google-genai>=2.0.0

# Pin langchain-core to 0.2.x for compatibility with langchain==0.2.16
langchain-core>=0.2.38,<0.3.0
